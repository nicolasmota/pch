# frontend

React 19 UI for Personal Context Hub. Built into `pcl-server` static assets. Node.js is **build-time only**.

```bash
cd frontend && npm ci && npm run build
npm run build:watch   # used by make serve / make desktop
npm run lint
```

Pages and routes: [The Hub UI](../docs/guides/the-hub-ui.md).
