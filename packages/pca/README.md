# pcl-pca

Portable Context Archive (PCA) export/import for Personal Context Hub. Depends on `pcl-core` only.

Export and import are owner HTTP/UI operations. This package also ships:

```bash
uv run pca verify-roundtrip <src_hub_dir> <dst_hub_dir>
```

Documentation: [Export and import](../../docs/guides/export-import.md)
